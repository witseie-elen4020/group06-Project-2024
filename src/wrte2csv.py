# A simple script used to help recod benchmark times for pdf extraction
import csv
import os

CSV_DR = test_times
def write_2_csv(method:str, size:int, files, extraction_times, job_times, fin_times, totoal_times):
